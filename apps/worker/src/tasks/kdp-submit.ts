import { definePlaceholderTask } from './_placeholder.js';

/** `kdp.submit` (F-041, Phase 3) — Phase 3 で本実装。 */
export const KDP_SUBMIT_TASK_NAME = 'kdp.submit';
export const kdpSubmitTask = definePlaceholderTask(KDP_SUBMIT_TASK_NAME);
