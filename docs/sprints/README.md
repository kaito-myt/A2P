# Sprints

> このディレクトリには `pm` エージェントがスプリントごとに 1 ファイル生成する。
> 命名規則: `SP-NN-{slug}.md`（例: `SP-01-bootstrap-monorepo.md`）。
> 指示の詳細は `.claude/agents/pm.md` を参照。

(未生成 — `docs/dev-plan.md` が記入され次第、各スプリントを順次作成)
